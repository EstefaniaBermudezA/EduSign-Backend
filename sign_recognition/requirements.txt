# Dependencias del módulo de reconocimiento de Lengua de Señas Colombiana (LSC)
# Instalar con: pip install -r requirements.txt

# Visión por computadora
opencv-python==4.10.0.84

# Detección de landmarks (manos y pose) con MediaPipe Tasks API
mediapipe==0.10.14

# Procesamiento numérico
numpy==1.26.4

# Red neuronal (1D-CNN) para clasificación de señas
torch==2.4.1

# Servidor WebSocket para integración con el cliente VR (Unreal Engine 5)
websockets==13.0
