# Copia este archivo como .env y completa los valores reales.
# NUNCA subas el .env a git (debe estar en .gitignore).

# Connection string de MongoDB Atlas
# Reemplaza TU_PASSWORD con el password real del usuario edusign_app
MONGO_URI=mongodb+srv://estefanibearroyo_db_user:TU_PASSWORD@edusign.n99oxtp.mongodb.net/?appName=edusign

# Base de datos donde se guardan las notas
MONGO_DB=edusign

# Colección de notas dentro de la base
NOTES_COLLECTION=notes
